#include <stdio.h>

/**
 * main - Print numbers from 0 to 10 without char function.
 *
 * Return: Always (0) success.
 */
int main(void)
{
	int a;

	for (a = 48; a < 58; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
