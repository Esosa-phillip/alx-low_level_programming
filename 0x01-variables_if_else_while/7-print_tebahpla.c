#include <stdio.h>

/**
 * main - Prints alphabets in reverse.
 *
 * Return: Always (0) success.
 */
int main(void)
{
	int a;

	for (i = 122; i > 96; i--)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
